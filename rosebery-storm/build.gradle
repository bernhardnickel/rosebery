description = 'storm integration for rosebery'

repositories() {
    maven {
        url "http://clojars.org/repo"
    }
}


dependencies {
    compile project(':rosebery-common')
    compile 'org.apache.storm:storm-core:0.9.4'
    compile "storm:carbonite:1.4.0"
}